@model IEnumerable<VidlyPrototype.Models.Movie>
@using Microsoft.AspNet.Identity;

@{
    ViewBag.Title = "Movies";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div id="movie-header-section">
    <div class="container movie-header-inner header-inner-spaced">
        <div class="row">
            <div class="col">
                <h3 class="display-4 border-bottom py-2 mb-3">Movies</h3>
                @*@if (Model.Any())
                    {*@
                <div class="table-responsive">
                    <table class="table table-bordered" id="movies">
                        <thead class="thead-light">
                            <tr>
                                <th>Name</th>
                                <th>Genre</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @*@foreach (var movie in Model)
                                {
                                    <tr>
                                        <td>@Html.ActionLink(@movie.Name, "Edit", "Movies", new { id = movie.Id }, new { @class = "text-capitalize" })</td>
                                        <td class="text-capitalize">@movie.MovieGenres.Name</td>
                                    </tr>
                                }*@
                        </tbody>
                    </table>
                </div>
                @*}
                    else
                    {
                        <p class="lead text-center">There are no movies at the moment</p>
                    }*@
            </div>
        </div>
    </div>
</div>
@section scripts
{
    <script>
        $(document).ready(function () {

            var table = $("#movies").DataTable({
                ajax: {
                    url: "/api/movies",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "name",
                        render: function (data, type, movies) {
                            return "<a class='text-capitalize' href='/movies/show/" + movies.id + "'>" + movies.name + "</a>";
                        }
                    },
                    {
                        data: "movieGenres.name",
                        render: function (data) {
                            return "<span class='text-capitalize'>" + data + "</span>";
                        }
                    },
                    {
                        data: "name",
                        render: function (data, type, movies) {
                            return "<button class='btn btn-link js-movie-rent' data-movie-id='" + movies.id + "'><i class='fas fa-shopping-basket text-success'></i> Rent</button>";
                        }
                    }

                ]
            });

            $("#movies").on('click', ".js-movie-rent", function (e) {
                e.preventDefault();

                var button = $(this);
                bootbox.confirm("Are you sure you want to rent this movie?", function (result) {
                    if (result) {
                        $.ajax({
                            url: "/api/userRentals/",
                            method: "POST",
                            data: { "movieId": button.attr("data-movie-id") },
                            success: function () {
                                //table.row(button.children("i").removeClass('fa-shopping-basket text-danger').addClass('fa-checkbox')).remove().draw();
                                toastr.success("Movie Added To Your Rentals List");
                            },
                            error: function (x) {
                                console.log(x.status);
                                if (x.statusCode != 201)
                                    toastr.error("Movie Not Added To Your Rentals List");
                            }
                        });
                    }
                });

            });
        });
    </script>

}

